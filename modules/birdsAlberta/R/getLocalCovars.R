
getLocalCovars <- function(covarTable = sim$covar, 
                           covarParams = sim$covarParams,
                           disturbanceMap = sim$disturbanceMap,
                           ageMap = sim$ageMap,
                           habitatMap = sim$habitatMap,
                           vegMap = sim$vegMap){
  
  browser()
  
  
  # 1. Get values from rasters for LOCAL
  
  
  return(invisible(sim))
}