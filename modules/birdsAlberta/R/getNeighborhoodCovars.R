
getNeighborhoodCovars <- function(covarTable = sim$covar, 
                                  covarParams = sim$covarParams,
                                  disturbanceMap = sim$disturbanceMap,
                                  ageMap = sim$ageMap,
                                  habitatMap = sim$habitatMap,
                                  vegMap = sim$vegMap){
  
  browser() 
  # 2. Get values from rasters NEIGHBORHOOD 
  
  return(invisible(sim))
}